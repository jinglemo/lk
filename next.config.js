/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
     // Set to true or false based on your preference
    // Other configuration options
  };
  
  module.exports = nextConfig;
  